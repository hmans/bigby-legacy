export const EPSILON = 0.000001

/**
 * The magic game development number.
 *
 * > "Gamedev secret: All of your favorite games have a line
 * > of code somewhere with an arbitrary magic number that's
 * > just fed into some formula because it feels right. The
 * > variable is called "fudge" and its value is always 0.65"
 *
 * (Aaron San Filippo)
 */
export const FUDGE = 0.65
